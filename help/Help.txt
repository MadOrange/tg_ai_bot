Запуск окружения
python -m venv venv
Запуск Бота
python3 tg_ai_bot_1.x.x.py


#Проверка связи с GigaChat curl -v --max-time 30 https://ngw.devices.sberbank.ru:9443/api/v2/oauth

 Как редактировать JSON правильно
Всегда используйте двойные кавычки " для ключей и строковых значений.

Экранируйте специальные символы: если внутри строки нужны кавычки " или переносы строк \n, используйте обратный слеш: \", \n.

Проверяйте запятые: после каждого элемента в объекте или массиве (кроме последнего) должна быть запятая.

Используйте валидатор: после правок проверяйте файл на JSONLint.

 В начале функции get_ai_response, сразу после строки kb_info = search_in_knowledge_base(user_text), добавьте:
  # Временная отладка
    print(f"=== DEBUG ===")
    print(f"Вопрос: {user_text}")
    print(f"Найдено в базе: '{kb_info}'")
    print(f"Есть ' — ' в ответе? {' — ' in kb_info if kb_info else False}")
    print(f"=== КОНЕЦ DEBUG ===")
	
	
	
	
	### 📦 5. Создание requirements.txt

Выполните в терминале в папке проекта:

```bash
# Убедитесь, что виртуальное окружение активировано
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# Создать файл со всеми зависимостями
pip freeze > requirements.txt